# Copy to `.env.production.local` before `vercel --prod` / `npm run build`
# (do not commit secrets; this file is an example only)

# WebSocket signal hub on Render (must be wss:// … /mp)
VITE_SBARG_SIGNAL_URL=wss://sbarg-nuketown-hub.onrender.com/mp
